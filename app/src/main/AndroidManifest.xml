<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gdg.rssreader">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".feed.MainActivity"
            android:label="@string/title_rssitem_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feed.RssItemListActivity"
            android:label="@string/title_rssitem_activity">
        </activity>
        <activity
            android:name=".feed.RssItemDetailActivity"
            android:parentActivityName=".feed.RssItemListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.gdg.rssreader.feed.RssItemListActivity" />
        </activity>
    </application>

</manifest>
